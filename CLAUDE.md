# CLAUDE.md: pointer file

Claude Code loads this file automatically, so it stays in place as a redirect. The canonical rules for every AI agent working in this repository live in [`AGENTS.md`](AGENTS.md). Read that file, not this one.

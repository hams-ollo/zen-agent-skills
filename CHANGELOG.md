# Changelog

Completed work for the Zen Starter Kit, newest first. This is the append-only ledger for the spec-driven task workflow: when a task in [`.tasks/`](.tasks/) is finished, its file moves to `.tasks/done/` and one dated line is added here referencing the task id.

---

## Task log

- [x] (2026-07-28) Ran `doc-sync` over the five reader-facing documents ahead of making the repository public, and applied all four findings. Ledgers (`CHANGELOG.md`, 48 archived task files) were skipped and the contract documents (`AGENTS.md`, `docs/spec/*`) were read and left unchanged; none disagreed with the code. **The substantive finding is one the kit has now made twice.** `README.md`'s layout table named `house-style.md` as the only rules module when two exist, which is the identical omission the 2026-07-27 `AGENTS.md` amendment fixed a day earlier, in a different document, for the same reason: `review-quality.md` holds the entire review rubric, and a layout table that omits it hides the file whose absence once shipped `house-review` with no rubric at all. Fixing the canonical file did not fix the copy of the claim in the README, which is the standing hazard in describing the same structure in two places. Also applied: the README described the catalog as listing "shipped, draft, and planned skills" when no draft has existed since `chore-0007` cleared the last stale claims, and the catalog's own preamble says everything is shipped or planned; the layout table omitted `docs/GETTING-STARTED.md`, the most approachable document in the repository, while listing it four sections earlier; and `docs/ARCHITECTURE.md`'s verification list gave four commands to run after changing a distribution path but not the test suite, though `tests/test_install.py` and `tests/test_build_adapters.py` are exactly what a distribution-path change breaks, and the same document calls `tests/` a first-class artifact. A contributor following that list would have skipped the tests that cover the code they just touched. Separately, and outside what `doc-sync` reports since quality is not drift, the README gained a "Where to start" routing table: five reader kinds, including the two agent cases the repository had never distinguished, an agent working *on* this repository (which reads `AGENTS.md` and follows its reading protocol) and an agent asked to *use* a skill (which reads that skill's self-contained `SKILL.md`). Every relative link in all five documents was checked to resolve, counts were re-derived from disk rather than from the prose, and the suite, both validators, and the link check all pass.
- [x] (2026-07-27) Amended `AGENTS.md`, the repository's contract document, on the author's explicit instruction after `doc-sync` reported it as `D-002` and left it unedited per the contract rule. Auditing the whole file rather than the one reported line found three gaps, not one. **The layout table listed one rules module when two exist**: `review-quality.md` shipped with `feat-0007` and was never added, so the canonical layout omitted the file that holds the entire review rubric, while a later section of the same document referred to it as a rules module. The entry now names both, says either may be replaced, and records that `install.py` ships them alongside the skills, because a skill that references a lens is not self-contained without it. **The `docs/spec/` entry named only conformance reports** when four report kinds now exist; it enumerates all four with the question each answers. **And the portability contract said nothing about link portability**, which is the substantive one: a skill ships as a directory alongside its siblings and the rules module, without this repository around it, so a link may reach its own files, a sibling skill, or `.agents/rules/`, and nothing above that. That rule has been enforced by `validate-skills.py` since 2026-07-27 and honored by `build-adapters.py`'s rewriting, but it existed only as a lint error and a code path, never as a stated rule, which is backwards for the file whose job is stating the rules. It was also the exact defect that shipped `house-review` with no rubric, so the entry names that failure and why it is silent. The stated boundary was checked against the validator's actual `portable_root` rather than asserted: both resolve to `.agents/`.
- [x] (2026-07-27) Ran `doc-sync` change-scoped to the day's 22 commits and applied six of its eight findings. 98 tracked Markdown documents in scope, 57 skipped as ledger, 3 contract documents read and left byte-for-byte unchanged. **The finding that mattered was an approval that had been claimed and never made**: `docs/spec/house-review.md` sat at `status: draft` while the `chore-0012` changelog entry and its commit message both stated it had been re-approved. The author had authorized the re-approval, the contract was amended, and the flag was never flipped, so `verifier-agent` would have returned `blocked` against it and the kit had seven approved contracts where its own ledger said eight. Neither the contract nor the ledger was editable under this skill's rules and neither was what was wrong: the fix was an action rather than a text correction, taken on the author's explicit instruction, which makes the existing ledger claim true rather than rewriting history. Also applied: `docs/ARCHITECTURE.md` and `README.md` both enumerated the `docs/spec/` report kinds as conformance and verification only, when the day added `<spec>.readiness.md` and `<spec>.characterization.md`; `ROADMAP.md` Epic A item 8 still said the three hardest spine branches "have never fired on real work" when all three had been exercised that day, and still called behavior-exercising "the harder half still open", so it now points at the second pass's findings section; and `docs/PROJECT-STATUS.md`, the partner-facing document, described a kit meaningfully behind the one that exists, with zero mentions of the rename, the three exercised branches, the eight contracts, or the test suite's growth from eighteen to forty-eight. One finding was dropped as `suspected` and verified accurate rather than stale: the roadmap's "5 of 19 skills will have a contract and none will have behavioral tests" is still exactly true, only its tense is dated. One `AGENTS.md` finding was reported and **not** applied, per the contract rule, since amending it needs the kind of explicit decision `chore-0011` had. The run also surfaced a class defect the initial sweep missed and a re-check caught: all four evaluation records name the task that produced them, and every one of those tasks had since moved to `.tasks/done/`, so four frontmatter pointers dangled. Repointed, since the file is the same and only its location changed. That will recur on every future closeout, and it shares a root cause with the `--strict` defect `chore-0012` fixed: paths that move when work completes.
- [x] (2026-07-27) Closed `chore-0017`: gave `scripts/install.py` the optional `argv` parameter that `validate-skills.py` (`chore-0003`) and `build-adapters.py` (`feat-0026`) already had, making its CLI layer reachable from a test, and covered the two approved scenarios that had no test because of it. Calling `main()` with no argument is unchanged, so every real caller behaves identically, and both new tests were confirmed to fail against the pre-fix `main()` rather than merely restating it. Suite 46 to 48; every scenario in `docs/spec/install.md` now has a covering test. **`S-010` is covered on one branch only, and the reason is a genuine constraint rather than a shortcut.** The task's own note had warned against a test that hardcodes `"copy"` and so silently passes everywhere else; the first attempt went further and patched `os.name` to exercise both branches, which fails outright, because `pathlib` selects `PosixPath` or `WindowsPath` from that same attribute and raises `NotImplementedError` on instantiation. Faking the platform is not available. The test therefore derives its expectation from the running platform and asserts the wiring end to end, failing if the default changes or the flag stops feeding it, while the opposite branch is exercised only by running the suite there. Recorded in the matrix as covered-on-one-branch with the reason, rather than as covered, since closing the other half means extracting the default into its own expression, a behavior-preserving refactor deliberately outside this task's scope.
- [x] (2026-07-27) Closed `feat-0029`: wrote and approved `docs/spec/install.md`, the last uncontracted kit script, so every distribution script now has a contract and a conformance matrix. Written in the documented order, since `feat-0027` pinned the behavior with characterization tests first and a spec written after a baseline describes rather than wishes. Eleven scenarios covering placement per tool, the rules module's location, re-run recognition, the never-overwrite rule, the lost-record consequence, preview, reversal, and invocation failure. Two judgment calls decided the contract's boundary. **The manifest is contract-level**, not an internal detail, because deleting it changes observable behavior, which `S-005` states; the module docstring had warned about it and no contract had ever said it was intended, which is exactly the class of behavior a future editor would "fix". **The two `feat-0027` testability findings are not contract items** and were deliberately excluded, since the manifest's location is intended behavior and argv injection is a property of the code rather than of what the tool does; putting them in would have been the describing-the-implementation failure the task warned against. `S-002` states the rules module's location by derivation rather than by path, going wherever the skills' existing references resolve to, which is the property the 2026-07-27 blocker violated and which a literal path would have made look arbitrary. `spec-quality` caught one gap before the draft was committed: `S-010` traced to no goal, the fourth consecutive spec whose orphan was an invocation concern after `validate-skills` S-015, `build-adapters` S-013 and `house-review` S-011, so goal 7 now covers working out of the box per platform. The suite went 8 to 10 and was promoted from characterization to acceptance: every test tagged with its scenario, the file relabeled, plus new tests for `S-005` and `S-011`. **The matrix converted a loose observation into a concrete one**: `S-009` and `S-010` have no test and cannot get one, because both live in `main()`, which calls `parse_args()` with no argv, so two named scenarios of an approved contract cannot be verified until `install.py` gets the injectable entry point `validate-skills.py` and `build-adapters.py` were both given when they were brought under test. Filed as `chore-0017`. Tests 44 to 46.
- [x] (2026-07-27) Closed `chore-0015`: classified the two kinds of emitted shared material in the `build-adapters` contract and stated how each is treated on a re-run, retiring the "behavior found outside the contract" section the `feat-0026` audit had to carry. **Decision: keep the asymmetry.** The contract already held the principle half-stated, since Non-Goals said adapters are derived and overwritten and Constraints said the rules module is swappable with the adopter's copy outranking the kit's; skill supporting files had simply never been classified as either. They are derived, existing in a target project only so an adapter's link resolves, while the adopter's real working configuration lives elsewhere in their repository, so replacing them loses nothing. A new Constraint states that ownership split, and no code changed. **Goal 3 was wrong, and nothing had caught it**: it read "without overwriting a copy the target project already has", generalising the rules behavior to all shared material, which is not what the tool does and never was. The `feat-0026` audit found the missing scenario and missed this, because a conformance matrix walks scenarios and surface elements and nothing walks the goals, so a goal can state something the code contradicts and no audit step will look at it. `S-014` states both halves in one scenario, with the deliberate overlap with `S-010` written into the spec, because the contrast is the requirement and stating either half alone leaves a reader unable to tell a rule from an accident. Its test asserts both halves together and was confirmed to **fail** against the symmetric alternative this task considered and rejected, which makes it an oracle over the decision rather than a restatement of current behavior: an editor who makes the loops symmetric is sent to the contract. Tests 43 to 44. All three readiness-gate gaps against this task are now closed, `source: plan` by `chore-0016`, `source: spec` by the scenario and constraint, and `source: both` by tracing to `S-014`. Worth recording that the gate had blocked a task whose entire purpose was to perform the resolution it demanded; that was correct as scoped, since it blocked implementation and an amendment is not implementation, but `spec-plan-readiness` assumes tasks implement against a contract and a task that changes the contract inverts that assumption.
- [x] (2026-07-27) Closed `chore-0016`: aligned the task format with what `spec-plan-readiness` actually requires, closing the second of two seams between the spine's front and back halves. **The audit found one gap, not several.** The task was deliberately scoped to re-read the gate's full Step 3 list rather than fix the one instance `feat-0028` hit and wait for a third, and doing so showed the seam is narrower than the two prior incidents suggested: five of the six requirements were already covered, by `depends_on`, `touched_files`, the acceptance section's command and success criteria, the `scenarios` frontmatter `feat-0025` added, and the acceptance section's test checkbox. Only risk and rollback notes were genuinely absent. An optional `## Risks and rollback` section now sits in both template copies, optional by design and saying so, because a heading every task carries and most leave blank teaches authors to skip it. The trigger is quoted from the gate rather than paraphrased, in the template and in `new-task`, since the two drifting apart is the failure being fixed; `new-task` adds that it is not a judgment call about how risky the work feels, and that "touches more than one module" is answerable from the `touched_files` just written rather than from intuition. **The gate's own `required_resolution` was then applied to the task it blocked**: `chore-0015` gained risk and rollback notes covering both branches of its open decision, closing its `source: plan` gap while its `source: spec` and `source: both` gaps remain, so it stays correctly blocked. Closing all three would have meant amending the contract, which is `chore-0015`'s own job.
- [x] (2026-07-27) Closed `feat-0027` and `feat-0028`, exercising the last two of the three branches ROADMAP Epic A item 8 named as never having fired on real work. **All three are now exercised on real triggers, none staged.** `feat-0027` ran `test-author` in characterization mode against `scripts/install.py`, the kit's only script with neither a contract nor a test, which matched the mode's documented trigger on every clause without arrangement. Eight characterization tests, all passing, `install.py` byte-for-byte unchanged, labeled as characterization in the docstring, the class name, and every test comment. Its value was in sequencing rather than coverage: the tests are unremarkable, but the pass produced a pinned baseline plus two testability defects the forthcoming `install.py` spec must now account for rather than describe around, namely that `MANIFEST` is a module-level constant pointing into the repository so any test calling `install()` writes into the real tree, and that `main()` takes no argv so the CLI layer is undrivable from a test. Both `validate-skills.py` and `build-adapters.py` were given injectable entry points when they were brought under test; `install.py` was not. Recorded at `docs/spec/install.characterization.md`. `feat-0028` asked `spec-plan-readiness` a real go/no-go question over the approved `build-adapters.md` and a genuinely-authored task (`chore-0015`, which states the shared-asset overwrite asymmetry the `feat-0026` audit found outside the contract). `verdict: blocked` with three gaps spanning all three `source` values, and it reached the non-short-circuit path, which is the branch worth exercising: both inputs were readable, so evaluation proceeded through the spec, plan, and consistency checks rather than returning the cheap missing-input blocker. Recorded at `docs/spec/build-adapters.readiness.md`. Two findings. **The most valuable gap needed two documents to see**: `chore-0015` reads as a complete task and `build-adapters.md` reads as a complete contract, and only holding them together shows the work has no scenario to trace to, which is the gate's actual argument for existing. **And a second seam between the spine's halves**: `chore-0015` was authored to the `new-task` bar hours earlier and still failed the deterministic risk rule, because `new-task` never prompts for risk or rollback notes and `_TEMPLATE.md` has no section for them, while `spec-plan-readiness` requires them for any task touching more than one module. The first such seam was task-to-scenario traceability (`feat-0025`); the pattern is that `spec-plan-readiness` was folded in from upstream against a different planning artifact and its requirements were never reconciled against the task format this kit authors. Filed as `chore-0016`, which is scoped to re-read the gate's full required list rather than fix this one instance and wait for the third. The blocked work was deliberately not started: a blocked verdict authorizes no implementation, so `chore-0015` stays open. Tests 35 to 43.
- [x] (2026-07-27) Closed `chore-0012`: renamed `code-review` to **`house-review`**, resolving the collision with the review commands several harnesses ship built in. The collision was confirmed real rather than hypothetical: this harness's own built-in review command describes itself as "for your working diff use `/code-review`". `house-review` was chosen over `zen-review` because it travels, since an adopter who is not Zen Solutions still reads it as "the review that uses my house rubric", which is exactly what the skill is, whereas a kit-branded name makes the skill feel like it belongs to its author rather than to them. The three "distinct from the built-in" assertions were deleted rather than reworded, and the blanket rename made the reason vivid: they briefly became "distinct from Claude Code's built-in `/house-review` command", which is not a command that exists. A skill cannot resolve a namespace collision by asserting it is different, so the intro now states the actual reason for the name instead. **Both Open Questions in the contract were resolved and it was re-approved**: a range supplied alongside a path scope narrows a change review to those paths rather than triggering a full-file review, now `S-012` tracing to goal 2, and the naming question closed itself; `Open Questions` now reads `None.` Two defects the rename exposed, both fixed. **`.tasks/validate.py --strict` was checking completed tasks' `touched_files`**: six archived tasks correctly recorded `.agents/skills/code-review/SKILL.md`, so the rename broke strict mode permanently and the only way to "fix" it would have been rewriting ledger files that `doc-sync` forbids editing. Strict mode now checks open work only, because a done task's `touched_files` are a record of what it changed rather than a claim about the tree today; the scaffold's template copy is synced, so adopters inherit the fix. **And the blanket rename hit two ledger documents** whose pointers then dangled, both repaired so every link resolves, with the verification record carrying a naming note recording that the run happened under the old name rather than silently reading as though it did not. Verified across both distribution paths: `install.py` places `house-review` with no stale `code-review` directory and its rubric still resolving, and `build-adapters.py` emits `house-review.mdc` with sibling cross-references rewritten. Archived task files and `CHANGELOG.md` keep the old name throughout, which is correct: they describe work done when the skill was called that.
- [x] (2026-07-27) Closed `chore-0011`: every one of the 19 skills now references the swappable house-style module, and `AGENTS.md` documents the two legitimate body shapes (workflow skills carry a procedure, lenses carry `Intent`/`Workflow`/`Output format` because they are composed rather than run) so the author of skill twenty does not have to guess. The functional half mattered more than the cosmetic one: a skill that never points at the module silently ignores an adopter who replaces it, which breaks the promise the kit makes about that file. **The task's own premise turned out to be wrong and the work corrected it.** It claimed two of the six skills needed target-repo-aware wording rather than the standard paragraph; checking each against its actual text found four. `pr-describe` was the sharpest miss, because its Notes already say "Do not hardcode this kit's own conventions into another repo", so pasting the standard paragraph in would have contradicted a rule stated in its own body, which is exactly the regression the task warned about while under-counting the skills it applied to. `fix-batch` was the fourth, since the prompts it dispatches point agents at the target repository's `AGENTS.md`. Only `reconcile-worktrees` and `spec-plan-readiness` were clean cases. Each of the four now states which conventions govern what: the artifact written into the target repository follows that repository's conventions, the skill's own report follows the house-style module. `AGENTS.md` records that split as the general rule. Worth noting that this is the `new-task` rule added earlier the same day, verify any claim you make about the code before writing it into a task, failing against its own author on a task filed two days before the rule existed; the premise had been written from a plausible reading rather than a check.
- [x] (2026-07-27) Closed `chore-0014`: resolved the ambiguity `feat-0024` surfaced by hitting it, and gave `verifier-agent` its first conformance audit. **Decision: accumulate.** A run whose spec is unapproved and which also has no runnable command satisfies both blocking preconditions, and the contract never said what to do. S-005 and S-006 each said "returns `blocked`" without covering the conjunction, while the plural `blocking_reasons` field implied accumulation and the skill body's "stop and return `blocked`" read as short-circuit. Both preconditions are now checked before returning, with every reason that holds reported in a fixed order (unapproved contract first, missing command second), specified as new `S-011`. The reasoning is that neither check runs the implementation, so evaluating both is free, while short-circuiting sends the reader away to fix one blocker and straight back for the second; the plural field was already the right shape and the prose was what was wrong. S-011 traces to goals 1 and 5, so no new goal was needed, which broke a three-spec run of `spec-quality` catching orphaned scenarios. The difference is instructive: the previous three orphans (`validate-skills` S-015, `build-adapters` S-013, `code-review` S-011) were all invocation or channel concerns, while this one is core behavior, so the blind spot is narrower and more predictable than "scenarios get missed". **`docs/spec/verifier-agent.conformance.md` now exists**, closing a gap the `feat-0024` run found by accident: the kit's most safety-critical skill had carried an approved contract since 2026-07-24 and had never been audited against it. All eleven scenarios and every surface element conform, and the matrix states plainly why that is weaker than it looks, since the contract and the skill were drafted the same day from the same intent, so a clean result mostly confirms the two documents still agree with each other. It also records the limit that applies to auditing any prose skill: conformance establishes that the skill **instructs** the specified behavior, not that anything **enforces** it, which is the same limit the `feat-0024` record names about exercising a branch. The observation in `code-review.verification.md` now points at this resolution, with its original single-reason record left as written, because a ledger entry describing a past run is not corrected to match a later contract.
- [x] (2026-07-27) Closed `feat-0024`: exercised `verifier-agent`'s `blocked` verdict on real work, the branch its own body calls the most damaging one to get wrong, and settled the evaluation-record format the two remaining unexercised branches will reuse. The trigger was deliberately not staged: `spec-author` drafted `docs/spec/code-review.md`, a contract the kit genuinely wanted since `code-review` is shipped, load-bearing, composes a swappable lens, and had none, self-checked to `ready` and written `status: draft` because only a human sets `approved`. Running `verifier-agent` against `code-review` while that contract sat unapproved is exactly the situation the branch exists to catch, where a fixture spec would have proven only that the branch can run. It behaved as `verifier-agent` S-005 describes on every clause: `verdict: blocked`, a blocking reason naming the unapproved contract, no command executed, no pass or fail reported for the implementation, and `code-review/SKILL.md` byte-for-byte unchanged by object hash. Recorded at `docs/spec/code-review.verification.md`, which doubles as the evaluation-record format: frontmatter naming the branch exercised, why the trigger is real, the skill's own output format verbatim, a clause-by-clause check against the scenario, and observations, kept to one page on the principle that a format costing more than the exercise is wrong. Eleven scenarios in the new contract, and `spec-quality` caught one gap first: S-011 traced to no goal, so a sixth goal now covers harness portability. That is the third consecutive spec where the lens caught an orphaned scenario, after `validate-skills` S-015 and `build-adapters` S-013, and all three orphans were invocation or channel concerns rather than core behaviors. Two findings recorded rather than resolved. **Both blocked preconditions were true simultaneously**: `code-review` also has no declared verification command, so S-006's trigger held alongside S-005's, and the skill's Step 1 reads as short-circuit while its plural `blocking_reasons` field reads as accumulation, with the contract silent on which is intended. Filed as `chore-0014`, which also notes that `verifier-agent` has an approved contract and has never been audited against it. **And "exercising a branch" of a prose skill is weaker than exercising a branch of a program**: the record now says so explicitly, because it is evidence that the procedure followed faithfully produces the specified outcome, not that anything enforces it. Filing `chore-0014` also surfaced a real tension between `new-task`'s honest-write-surface rule and `.tasks/validate.py --strict`, which requires every listed path to exist and therefore fails any create-a-file task by construction; noted in that task rather than worked around.
- [x] (2026-07-27) Closed `feat-0026`: wrote `docs/spec/build-adapters.md`, the missing contract for the one kit script that had tests and no spec, and the reason the 2026-07-27 `doc-sync` run had to soften three "derived from the specifications" claims instead of fixing them. Those claims are now restored and true: both test-backed scripts have approved contracts. A retrospective spec, so its value is not documentation but that rules living only in code became reviewable. Thirteen scenarios covering the two adapter shapes and their frontmatter, all three link-rewrite classes plus the two that travel unchanged, anchor and link-title preservation, emission of the shared material, the never-overwrite rule for the rules module, the same-file no-op, preview-writes-nothing, and rejection of an unrecognized target. Every scenario was verified against real execution before being written rather than described from the code. The `spec-quality` self-check caught two coverage gaps before the draft was committed: S-013 traced to no goal, so a sixth goal now covers failing clearly on an unusable invocation, and the surface's "exit zero otherwise" plus the summary output had no scenario, now folded into S-001. That is the second consecutive spec where invocation-level behavior was the blind spot, after S-015 in `validate-skills`. Tests grew 30 to 35: five scenarios had no coverage and all five were closed once the contract was approved, so every scenario now has a covering test. The S-011 test targets the repository itself and so uses a preview run, asserting the no-op without being able to write into the repo even if the no-op regressed. **The conformance audit found behavior outside the contract**, recorded in the matrix rather than silently passed: the two shared-asset loops guard differently. A rules file that already exists is skipped, which is S-010, while a skill's supporting file is overwritten, so an adopter who edits an emitted template loses it on the next regeneration. Confirmed by execution on both. It is defensible and probably intended, since a skill's templates are derived from the kit while the rules module belongs to the adopter, but it is an unstated contract decision sitting one line from a stated one and invisible to a reader of either the spec or the tests. Flagged with a recommendation and left for a human call, which is the third instance of implementation drifting past contract caught this session and the first caught during the audit that introduced the contract rather than days later.
- [x] (2026-07-27) Closed `feat-0025`: exercised the hardened batch loop end to end on a repository with real dependencies, the gap that made the whole 2026-07-27 pass cold. Target was `gaudiya-vaishnava-knowledge-wiki`, a `uv`-managed Python project with a gitignored `.venv`, chosen because this kit is documentation and stdlib Python and therefore cannot produce the conditions these procedures were written for. Three parallel worktree agents added first test coverage to three untested modules (`bg_parse`, `qa_check`, `validate_content`); the suite went from 9 tests to 47, and every diff was verified independently against real command output before anything was applied. **The reconciliation fix proved load-bearing rather than theoretical:** every item's deliverable was a new file, so all three `git diff` patches were 0 bytes, and under the pre-2026-07-27 `git diff | git apply` mechanism the entire batch would have landed nothing, reported success, and been deleted at cleanup. **Environment strategy** was install-per-worktree, measured at 2 seconds and 52MB per worktree, effectively free with `uv`'s shared cache; the skill now says to measure rather than assume, because the same choice against a cold `npm ci` is a different decision. Six findings folded back in. `fix-batch`: the Claude Code section assumed a same-repo batch, but `isolation: "worktree"` worktrees the *current* project and does nothing when the work is elsewhere, so cross-repo dispatch now has its own documented path; "tracked by git" became "committed", because a worktree checks out a commit and a staged-but-uncommitted task file is as absent as a gitignored one, which is exactly how three agents were stranded on 2026-07-25; the pre-flight instruction was strengthened from "run it or read it closely" to "actually run it", after it caught a genuinely broken acceptance command (`uv run pytest` fails where `uv run python -m pytest` works, because the console script does not put the repo root on `sys.path`) that would otherwise have produced three identical confusing blockers; and keeping closeout bookkeeping out of agent prompts became explicit doctrine, since those touch the same one or two files for every item and are the easiest collision in the batch to avoid. `reconcile-worktrees` recorded the 0-byte measurement, since "the work is new files" is a large fraction of real tasks and the tracked-diff half returns nothing in every one of them. `new-task` gained a rule to verify claims made *about* the code before writing them into a task: two of three task files carried premise errors written from a quick read (a function credited with logic it does not contain, a regex credited with handling a case it silently drops), and both agents caught and reported them rather than complying, which is the honest-blocker instruction working. The run also surfaced two real defects in the target repo, both reported and not fixed: an anchored wikilink like `[[bhakti#definition]]` does not match `WIKILINK_RE` at all, so `validate()` can neither confirm nor flag broken anchor links, and `KNOWN_PURPORT_LESS_VERSES` is defined but referenced nowhere. One acceptance criterion is recorded **unmet**: no deliberate exclusion occurred during reconciliation, because no spurious path appeared. The criterion presumed a condition this batch did not produce, and manufacturing an exclusion to tick it would have tested nothing.
- [x] (2026-07-27) Closed `chore-0013`: brought the `validate-skills` contract back in line with its implementation, and found a live defect doing it. The spec was reopened to `draft`, amended with two goals, two non-goals, one constraint, and scenarios S-009 through S-016, self-checked to `ready` against `spec-quality`, and re-approved by the author. The load-bearing addition is the constraint, not the scenarios: it defines the distributed skill tree, which turns S-011 into a consequence of how skills ship rather than an arbitrary rule about path depth, and makes S-012 (rules-module links are legal) the same rule read the other way. S-016 was one scenario beyond the seven drafted, because specifying S-015 without it would have left the adjacent case unstated and the contrast is the contract: an empty directory is a legitimate zero-skill success, an unreadable one is not. Both were verified by execution rather than inspection. The regenerated matrix then caught **S-014 diverging**, in a scenario approved the same day: the contract states a semantic condition ("asserts it is a draft and also records that it shipped") while the implementation matched two literal phrasings, so a skill saying "remains a draft" beside "- Blessed 2026-07-24" passed silently. Probed against five plausible phrasings, the shipped check flagged one and missed four. It was recorded as disposition `fix` rather than accepted-with-reason, because accepting it would have excused the drafting rather than the implementation, and because unlike S-008 the gap was closable. The patterns were then widened to cover assertion forms (`is`/`remains`/`stays` a draft, a `status: draft` line, `draft pending`) and provenance forms (a `shipped`/`blessed` list item, or either word before an ISO date). Neither half needs to be precise, because the finding requires both, and that conjunction is what keeps the check specific; narrow patterns were the actual defect. Verified three ways: all five phrasings now flag, four negative cases still produce nothing (including prose that merely discusses drafts, and a skill whose `status: draft` refers to a spec it authors rather than to itself), and the widened check still catches all three real historical instances that `chore-0007` fixed by hand, which is the genuine bug population. Zero false positives across the 19 current skills. Tests grew 29 to 30: the single-phrasing S-014 test became table-driven over the whole population, S-015 and S-016 gained the coverage the matrix had flagged as absent, and the link tests were tagged with the scenario ids the amended contract gives them. The S-014 table was confirmed to fail on four of its five cases against the real pre-fix patterns; the canonical phrasing always passed, which is precisely how a single-case test hid the divergence for two days. Every scenario except S-008 now has a covering test, and S-008 stays untested by design.
- [x] (2026-07-27) Ran `doc-sync` change-scoped to `8041160..HEAD`, the two commits of the 2026-07-27 hardening pass, and applied six of its eight findings. 80 tracked Markdown documents in scope, 37 skipped as ledger (`CHANGELOG.md`, 35 archived task files, the two conformance and verification reports), 8 contract documents read but never edited. Six of the eight findings were drift this pass had itself introduced, which is the case the skill is cheapest at and a human re-reading familiar prose is worst at. **Applied:** `D-001` and `D-002`, the "seven skills carry no house-style pointer" count in `ROADMAP.md` and in `chore-0011`'s own instruction table, both now six after `spec-conformance` gained a `## Conventions` section; `chore-0011` was also unblocked in prose (`feat-0022` and `chore-0010` are done), had `spec-conformance` dropped from its `touched_files`, and gained a warning that three of its target skills were edited on 2026-07-27 and should be re-read rather than worked from its original picture. `D-003`, the `spec` and `scenarios` frontmatter fields missing from this repo's own `.tasks/README.md`: the scaffold template got them and the kit's own copy did not, so the kit was briefly shipping better documentation to adopters than it kept for itself. `D-004`, `docs/ARCHITECTURE.md`'s enumeration of `validate-skills.py` checks, which read as exhaustive and omitted the new shipped-tree escape check. `D-006`, `docs/PROJECT-STATUS.md`, dated 2026-07-25 and describing the first review pass as the only one; it now covers both passes, states the two silent blockers in partner-facing terms, and names the structural limit behind them, that a repository of documentation and stdlib Python cannot exercise procedures written for ordinary software projects. `D-007`, three claims across `README.md` and `docs/ARCHITECTURE.md` that the test suite is "derived from the specifications under `docs/spec/`", falsified by this pass's own `tests/test_build_adapters.py`, which has no spec; softened, with the gap named rather than papered over. **Not applied:** `D-005` (`docs/PLATFORM-PITCH.md:200`, `suspected`, dismissed because a one-line inventory bullet is not an exhaustive list). `D-008` is a **contract** finding and was reported only, never edited: `docs/spec/validate-skills.md` is `status: approved` and has no scenario covering the escape check the implementation now performs. This is the second time the validator has grown past its approved contract (`feat-0023` was the first and recorded the same note), so the pattern now wants a decision, amend and re-approve the spec or record an accepted divergence in the conformance matrix, rather than a third one-off note. Every applied finding was re-checked against the tree before writing and re-verified as resolved after.
- [x] (2026-07-27) Closed the second half of the 2026-07-27 pass's Blocker 1, in `scripts/build-adapters.py`. The earlier fix repaired `install.py` but left the Cursor and VS Code path broken in the same way and for the same reason: an adapter is generated into `.cursor/rules/` or `.github/prompts/`, which is not where the skill sits, so inlining a body verbatim broke every relative link in it. Measured against the real kit, that was 97 dangling links per target, 194 across both, and it left `code-review` with no rubric on two of the kit's three distribution paths. Three link classes are now rewritten as the body is inlined: a sibling skill becomes the adapter generated beside it (`../fix-batch/SKILL.md` to `fix-batch.mdc`, anchors and link titles preserved), the rules module becomes `../../.agents/rules/<file>`, and a skill-local template becomes `../../.agents/skills/<name>/<path>`. Both adapter directories sit two levels below the project root, so one shared location serves both targets instead of one copy per target. The material those links now point at is emitted alongside: the rules module and each skill's supporting files, never overwriting an existing `.agents/rules/` file, because that module is swappable and a project's own copy outranks the kit's, and skipping any copy where source and destination are the same file so a build into the kit itself is a no-op. `main()` takes an optional `argv` so it can be driven from a test, mirroring the injectable `skills_dir` that `chore-0003` added to `validate-skills.py`. New `tests/test_build_adapters.py` covers the script, which had none: six unit tests over `rewrite_links` for each link class plus the two that must travel unchanged, and three component tests that emit the real kit into a temp directory and resolve every relative link on disk, which is the layer the defect actually lives at. Suite 20 to 29. The component test was confirmed to fail against the pre-fix behavior (7 failures and 1 error with rewriting and asset emission disabled) rather than assumed to. `README.md`, `docs/ARCHITECTURE.md`, and `docs/GETTING-STARTED.md` were resynced, the last for the tier claim that this batch itself invalidated by moving `validate.py` to every tier.
- [x] (2026-07-27) Ran the second kit-wide review pass, the first to go at skill *mechanics* rather than structure, and landed the fixes directly. The 2026-07-25 pass deliberately reviewed structure, status claims, and cross-references; this one stress-tested each procedure against what happens on a real repository, and found two blockers, five majors, and eight smaller issues, none of which either validator could see. **Blocker 1:** `scripts/install.py` shipped the skills without `.agents/rules/`, so 14 of 19 skills carried references that resolved in this repo and dangled once installed, and `code-review` lost its entire rubric (severities, the eight categories, the validate-before-reporting protocol all live in the lens). Fixed by installing the rules module as the sibling `<base>/../rules`, which is exactly where each skill's `../../rules/<file>.md` already pointed, verified end to end into a scratch home. The three links that escaped even that tree (`../../../AGENTS.md`, `../../../.tasks/`) now name their target in prose, since they refer to the adopter's repo and were never resolvable. **Blocker 2:** `reconcile-worktrees` Step 5 applied worktrees with `git diff | git apply`, which shows neither untracked files nor staged edits and mangles binaries; since `fix-batch` requires agents to leave work uncommitted, every new source and test file an agent created was untracked, landed nothing, reported success, and was deleted with the worktree in Step 8. Steps 2, 5, and 8 now enumerate tracked, staged, and untracked changes separately, apply via a checked patch file plus an explicit untracked copy, and reconcile landed paths against the recorded `git status` list before any worktree is removed. The three underlying git behaviors were verified directly rather than asserted. **Majors:** `fix-batch` had no doctrine for what git does not carry into a worktree (`node_modules/`, `.venv/`, `.env`), which would have stalled every item of every batch on any real Node or Python repo behind a `blocked` verdict that looks like nothing is wrong; Step 2 now resolves it once for the batch with four named strategies. `test-author` was orphaned, wired into no skill's procedure despite being the thing that produces the evidence `verifier-agent` consumes; it is now part of the Step 3 dispatch prompt. `new-task` was spec-blind while `spec-plan-readiness` blocks on task-to-scenario traceability, so the spine's front gate could not pass by construction; task frontmatter gained optional `spec` and `scenarios` fields (validated), and `new-task` gained a spec-decomposition section and a handoff to the readiness gate. `code-review` and `doc-sync` had no context budget, so a large diff or doc set produced a silently truncated pass indistinguishable from a complete one; both now bound scope up front and report coverage, and `doc-sync` gained a `not_audited` field distinct from `skipped`. The `lite` tier scaffolded three files referencing a `validate.py`, `ROADMAP.md`, and `CHANGELOG.md` it never wrote, while three sibling skills instruct agents to run that validator unconditionally; `validate.py` now ships at every tier (team adds enforcement, not the file) and lite-tier stripping is spelled out per file. **Minors:** both worktree skills read a base commit from `git worktree list`, which reports current `HEAD`; `fix-batch` now records the dispatch sha. `spec-author` hardcoded `docs/spec/` with no discovery step. `spec-conformance` cited a "closeout gate" this kit never defined, and gained a house-style pointer. POSIX-only `ls -la` and a patch pipe that corrupts under Windows PowerShell 5.1 were replaced with portable git commands. Kit-internal "Shipped 2026-07-24" provenance was removed from three skill bodies that adopters install. `scripts/validate-skills.py` now validates links against the shipped layout, not just the authoring one, so this whole class fails a command instead of waiting for a human read; test suite grew from 18 to 20. Two items were filed rather than decided: `chore-0012` (the `code-review` name collides with Claude Code's built-in) and `feat-0025` (none of the batch-loop hardening has met a repo with real dependencies, which is exactly why the trap survived to be found by reading).
- [x] (2026-07-25) Closed `feat-0022`: wired `verifier-agent` into `fix-batch` Step 6 and `reconcile-worktrees`, the follow-up `feat-0019` deferred until the skill had been used. Step 6 now composes `verifier-agent` by reference for the per-implementation verdict and states what `pass`, `fail`, and `blocked` each mean for a batch, including that `blocked` is not a pass and must not be reconciled. The four batch-specific checks that `verifier-agent` has no notion of were deliberately preserved rather than deleted as duplication: scope-creep diffing against the base commit, bookkeeping verification against what the agent claimed, the "I recovered from an error" high-scrutiny flag, and the cross-worktree landmine check for identical unexpected diffs implying a tooling issue. The independence rule is now stated once instead of twice. `reconcile-worktrees` no longer routes the reader to `fix-batch` Step 6 for verification. The implementing agent was terminated early by a spend limit before it could run any verification, so its diff was verified and the suites run by the orchestrator instead.
- [x] (2026-07-25) Closed `chore-0010`: `spec-plan-readiness` step 6 now composes `test-quality`'s layer selection by reference instead of restating the taxonomy inline. The inline copy had already drifted, silently dropping the lens's sixth layer ("Raw real-shape fixtures"), which is the failure mode the kit's compose-by-reference rule exists to prevent. Step 6 keeps what is genuinely local to it: a layer plus a reason per mapped scenario, preference for the lowest faithful layer, deference to the repo's own taxonomy, and an empty map when readiness was blocked before mapping. Readiness checklist item 11 was aligned to the same deference order. The `repoprompt-workflows` provenance line is unchanged. This also matters for adopters: the lens is swappable, so a restated copy would silently disagree with a retuned `test-quality`.
- [x] (2026-07-25) Ran `doc-sync` across the kit's own reader-facing documentation and applied the findings, bringing every current-state document into line with the 19-skill repository. `README.md` gained `doc-sync` in its workflow diagram and narrative, an accurate description of what `validate-skills.py` now checks, and the two verification commands it had never listed; its claim that the kit has no test suite was outright false and is corrected. `docs/ARCHITECTURE.md` mentioned neither `verifier-agent` nor `doc-sync` and now describes both, including the document classification model and the `<spec>.verification.md` artifact. `docs/CATALOG.md`'s "All six were blessed" tally sat above an eight-row table, and `init-worktracking` and `new-task` carried "shipping (Phase 1/2)" labels while every peer said "shipped". `docs/PROJECT-STATUS.md` was dated a day stale, undercounted the spine at six, and still listed `doc-sync` as an unbuilt future thread. `docs/GETTING-STARTED.md` and `docs/PLATFORM-PITCH.md` both omitted `doc-sync` from their skill inventories and collaboration diagrams. Contract documents (`AGENTS.md`, `docs/spec/*`, `.agents/rules/*`) were left unedited per the skill's own rule, and `CHANGELOG.md` was appended to rather than revised.
- [x] (2026-07-25) Closed `bug-0002`: replaced every `AGENTS.md` section-NUMBER reference with a reference by section NAME across `new-task`, `init-worktracking` (plus its `_TEMPLATE.md.tmpl` and `tasks-README.md.tmpl`), `fix-batch`, `spec-plan-readiness`, `.tasks/_TEMPLATE.md`, and `.tasks/README.md`. This was a genuine cross-repo portability defect: the numbering differs between this repository and the repos `init-worktracking` scaffolds, so `new-task` was correct for a scaffolded repo and wrong here while `spec-plan-readiness` was correct here and wrong there, and `.tasks/_TEMPLATE.md` propagated the wrong pointer into every task file authored from it. The agent found a third instance the task file had missed (`init-worktracking/SKILL.md:32`, "sections 1 and 5 still describe the model"). The out-of-scope renumbering of `AGENTS.md` itself was left alone. Reconciliation also fixed the same defect in `ROADMAP.md`'s header, which was outside the task's `touched_files`.
- [x] (2026-07-25) Closed `chore-0007`: cleared seven stale status claims across six shipped skills. Three (`code-review`, `pr-describe`, `project-bootstrap`) asserted they were a draft pending field iteration while also recording that they shipped 2026-07-24; the draft assertion was dropped and the settled-decisions framing kept. Four more called shipped siblings planned or future (`test-quality` on `test-author`, `code-review` on `test-quality`, `spec-conformance` on `verifier-agent`, `spec-quality` on `spec-author`). `code-review`'s "Future direction (not built yet)" note was left alone, since that skill genuinely is unbuilt.
- [x] (2026-07-25) Closed `feat-0023`: extended `scripts/validate-skills.py` with three checks the kit had been catching by human reading. Unresolved relative links and dangling sibling-skill references are errors; a skill asserting both draft and shipped status is a warning, since that phrasing is a judgment call and a false positive should not break a build. Link resolution skips `http`, `https`, and `mailto` and strips anchors. Test suite grew from 11 to 18. The dangling-sibling check would have caught, by command, the `document` references that survived until `chore-0006` fixed them by hand months later. Run against the tree at its pre-fix base, the new draft/shipped check independently reported exactly the three contradictions `chore-0007` was fixing in parallel, and the combined result is clean. `docs/spec/validate-skills.md` was left unchanged: the new behavior goes beyond its approved scenarios, and amending an approved contract is a separate human decision.
- [x] (2026-07-25) Closed `feat-0021`: iterated `doc-sync`'s detection guidance from four drift instances its own dogfood missed. Claim matching is no longer line-scoped, because prose wraps and a stale qualifier routinely lands on a different line from the thing it qualifies (`test-quality/SKILL.md:10-11` was missed for exactly that reason). The staleness vocabulary is now derived from the document set being audited rather than assumed, with "draft" named explicitly, and widening it explicitly does not widen what counts as evidence. Intra-document contradiction is now a named detectable class classified `grounded`, since the document supplies both halves of its own evidence and no repository lookup is needed. The approved contract `docs/spec/doc-sync.md` was left byte-for-byte unchanged.
- [x] (2026-07-25) Closed `chore-0008`: cross-linked `doc-author` and `doc-revise` to `doc-sync`, in both bodies and both frontmatter descriptions. The documentation trio had only linked one way: each sibling redirected to the other for the case it did not handle, but neither covered the case `doc-sync` now owns, which is not yet knowing which document is wrong. `doc-revise` also now records that `doc-sync` composes its editing discipline, so a future editor knows the file has a downstream consumer.
- [x] (2026-07-25) Closed `chore-0009`: made `agent-handoff` harness-neutral, removing "Claude Code" from its frontmatter description and body and replacing the "some sonnet agents" trigger phrase with a vendor-free equivalent. Nothing in the skill is actually harness-specific (it writes a Markdown brief), so the lock-in sat in the kit's most visible field and contradicted the portability contract. No harness-gated section was added, because there is no single-harness capability here to gate.
- [x] (2026-07-25) Ran the 2026-07-25 kit hardening batch through `fix-batch`: six task files dispatched to parallel Sonnet agents in isolated worktrees, then reconciled. Three agents opened their worktree to find no task file, because the harness cut every worktree one commit behind the commit that filed them; all three stopped and reported a blocker rather than merging main into their worktree or guessing, which is the behavior the hardened prompts exist to produce. Two were unblocked with a read-only `git show <commit>:<path>` against the shared object store; two whose worktrees had been auto-cleaned were re-dispatched with their task content embedded in the prompt. Two agents drifted outside their worktree on reconnaissance reads before the isolation guard blocked their writes, and both self-reported it; the main checkout was verified clean and unmoved in each case. Every diff was checked against its task's `touched_files`, no two agents touched the same file, and all three suites were re-run on the combined state rather than trusting six reported passes. Closeout bookkeeping was deliberately excluded from every agent prompt and done centrally, so the six could not collide on `CHANGELOG.md` and `.tasks/`.
- [x] (2026-07-25) Blessed `doc-sync` as **shipped**, closing the documentation half of the delivery spine. Blessed after the dry-run dogfood recorded below and after `chore-0006` exercised the apply path that the draft had left unexercised. Status updated in `ROADMAP.md` and `docs/CATALOG.md`. User sign-off recorded.
- [x] (2026-07-25) Closed `chore-0006`: repointed the three dangling `document` references at `doc-sync` (`spec-conformance/SKILL.md:3` and `:45`, `spec-quality/SKILL.md:51`), the known-answer targets the `feat-0020` dogfood was designed to surface. Run as `doc-sync`'s first apply pass, which exercised S-003 (only approved findings applied), S-011 (composing `doc-revise` for the edit) and S-014 (audit trail) for the first time: three findings approved by id out of twelve reported, three lines changed across two files, the other nine findings left reported and unapplied. All three were `grounded` findings evidenced by the absence of any `.agents/skills/document/` directory. The unbackticked occurrence inside a YAML `description` field confirmed the dogfood's lesson that frontmatter is prose and goes stale like any other.
- [x] (2026-07-25) Closed `feat-0020`: authored the `doc-sync` skill (ROADMAP Epic B #9), the documentation half of the delivery spine. It detects documentation drift by checking prose claims against repository facts, classifying every document by property as current-state (correctable with explicit per-finding approval), contract (report-only, because a disagreement there means the code is wrong) or ledger (skipped, because history is not drift), with an unclassifiable document defaulting to contract so the failure mode is inaction. Dry run is the default rather than a flag, every reported finding names the fact that grounds it, an ungroundable suspicion is not reported at all, and each finding carries a stable `D-NNN` id plus a `grounded` or `suspected` confidence so dismissing a false positive costs one identifier. Applying leaves an audit trail naming the claim corrected, the evidence, and the confidence it carried; vendored third-party material is never edited even when its path is named. Composes `doc-revise` by reference for editing discipline. Built against `docs/spec/doc-sync.md`, the second spec drafted by `spec-author` and explicitly approved before decomposition, after two open questions were resolved into the contract. The roadmap line crediting an upstream `document` workflow was corrected: no such skill was ever vendored here, so this was authored rather than folded in. Dogfooded and iterated; draft pending user sign-off before blessing.
- [x] (2026-07-25) Dogfooded `doc-sync` in dry-run mode across this repository's own documentation set: 67 tracked Markdown documents classified, 38 audited (29 current-state, 9 contract), 29 skipped as ledger, 12 findings, `verdict: drift_found`, no file modified. It independently surfaced all three dangling references to a `document` skill that does not exist in this kit (`spec-conformance/SKILL.md:3` and `:45`, `spec-quality/SKILL.md:51`), correctly classified `docs/spec/*.md` as contract documents it will not edit, and skipped `CHANGELOG.md`, the 26 archived task files, and the two audit reports as ledger. It also found five drift instances not anticipated by the known-answer set, including a stale "a future `test-quality`" in `code-review/SKILL.md:110` and a "All six were blessed" tally above a seven-row table in `docs/CATALOG.md:27`. The run's honest weakness was mechanical precision: the reference heuristic returned 13 hits for 1 true positive, and two of the three known-answer targets were found only by reading, one because the reference was unbackticked inside frontmatter and one because markdown bold broke the count pattern. The skill was iterated from that evidence to search on names rather than their decoration, to verify counts by counting the referent, to confirm what kind of thing a reference claims exists, and to classify audit and verification reports as ledger, since a report quoting what a command printed on a past date is a record and not a stale claim. Findings left unrepaired by design; the dangling `document` references stay in place pending sign-off.
- [x] (2026-07-24) Blessed `verifier-agent` as **shipped**, completing the core spec-to-reconcile loop. Dogfooded on `scripts/validate-skills.py` against its approved spec and the `chore-0003` acceptance criteria: both declared commands passed, all four criteria returned `met` with named evidence, and the single unreconciled conformance item (S-008) was correctly carried as accepted-with-reason rather than failing the run, producing a `pass` verdict recorded at `docs/spec/validate-skills.verification.md`. The run caught a live defect neither the tests nor a fresh audit would surface: every evidence citation inside `main()` in the conformance matrix had drifted +8 lines after the `chore-0003` refactor, classifications still correct on stale pointers. It reported the drift rather than repairing it, leaving every verified file byte-for-byte unchanged, and the skill was then iterated from that evidence to require checking a pre-existing conformance report's citations before trusting them. Blessed with the `blocked` verdict path specified but not yet exercised on real work, which the new kit-wide evaluation roadmap item is intended to cover. Status updated in `ROADMAP.md` and `docs/CATALOG.md`. User sign-off recorded.
- [x] (2026-07-24) Closed `chore-0005`: re-anchored every evidence citation in `docs/spec/validate-skills.conformance.md` from line references to `main()` and `parse_frontmatter()` symbol and branch references, fixing the +8 line drift the `verifier-agent` dogfood found after the `chore-0003` refactor. No classification, status, or disposition changed: all ten Conformed rows and the single accepted-with-reason S-008 divergence stand as first recorded. Symbol references were chosen because they survive unrelated edits above them. Acts on the durable-citation recommendation in the verification report.
- [x] (2026-07-24) Closed `feat-0019`: authored the `verifier-agent` skill (ROADMAP Epic B #7), the last core skill of the delivery loop. It runs the declared verification commands and records each exact outcome, composes `spec-conformance` so a to-fix divergence withholds a passing verdict even when every command passes while an accepted-with-reason divergence does not, maps each acceptance criterion to `met` or `unmet` with named evidence, and returns a deterministic `pass | fail | blocked` verdict. It returns `blocked` rather than guessing when a spec is unapproved or no command is declared, so a verification that could not run is never recorded as a pass, and it never edits the implementation, tests, or spec it verifies. Built against `docs/spec/verifier-agent.md`, the first spec drafted by `spec-author` and explicitly approved by the user before decomposition. Draft pending its dogfood.
- [x] (2026-07-24) Dogfooded `verifier-agent` on `scripts/validate-skills.py` against its approved spec and the `chore-0003` acceptance criteria: both declared commands passed, all four criteria came back `met` with named evidence, and the one unreconciled conformance item (S-008) was correctly carried as accepted-with-reason rather than failing the run, for a `pass` verdict recorded at `docs/spec/validate-skills.verification.md`. The run caught a real defect neither the tests nor a fresh audit would surface: every evidence citation inside `main()` in `docs/spec/validate-skills.conformance.md` had drifted +8 lines after the `chore-0003` refactor, with classifications still correct on stale pointers. It reported the drift instead of repairing it, leaving every verified file byte-for-byte unchanged. The skill was then iterated from that evidence to require checking a pre-existing conformance report's citations before trusting them. Remains a draft pending user sign-off before blessing.
- [x] (2026-07-24) Closed `chore-0004`: aligned the reader-facing documentation with the shipped Epic B spec/verify spine. `README.md` and `docs/PLATFORM-PITCH.md` spine diagrams now include the specification, test-authoring, and conformance stages; `docs/CATALOG.md` lost its duplicate `test-author | planned` Tier B row and its two stale "the planned X" cross-references; `docs/PROJECT-STATUS.md` and `docs/PLATFORM-PITCH.md` now report the six spine skills as shipped rather than as roadmap items; `docs/GETTING-STARTED.md` gained a spec-first on-ramp for founders; and `AGENTS.md` plus `docs/ARCHITECTURE.md` now record `docs/spec/` and `tests/` as first-class artifacts. Documentation-only: no skill body changed, `python scripts/validate-skills.py` and `python .tasks/validate.py --strict` both exit 0.
- [x] (2026-07-24) Blessed `test-author` as **shipped** after dogfooding it to derive the acceptance test suite for `scripts/validate-skills.py` from `docs/spec/validate-skills.md`: 11 standard-library `unittest` tests, each tagged with the scenario id it covers, at the lowest faithful layer (parser unit tests plus component tests over temporary fixture directories), all passing. It correctly omitted the accepted S-008 divergence rather than writing a fake passing test. Status updated in `ROADMAP.md` and `docs/CATALOG.md`. User sign-off recorded.
- [x] (2026-07-24) Closed `chore-0003`: gave `scripts/validate-skills.py` its first test coverage. Made the scanned skills directory injectable (`main(skills_dir=...)`, with the default CLI output byte-identical via a repo-relative display helper) and added `tests/test_validate_skills.py`, the `test-author` dogfood output. Acceptance: `python -m unittest discover -s tests` exits 0 (11 tests). Establishes the kit's `tests/` directory.
- [x] (2026-07-24) Closed `feat-0018`: authored the `test-author` skill (ROADMAP Epic B #5) by extracting the discipline from the upstream RPCE Test workflow into a portable `SKILL.md`. It gates the input spec with `spec-quality`, discovers and matches the repo's own test framework, maps each scenario to tests tagged by `S-NNN` while composing `test-quality` for layer and oracle, supports acceptance and characterization modes, proves bug-fix regressions fail first, reports coverage gaps instead of faking passes, and never edits production code. Built against its own contract (`docs/spec/test-author.md`). Draft pending an in-kit dogfood (deriving the `validate-skills.py` tests of `chore-0003`) before blessing.
- [x] (2026-07-24) Blessed `spec-author` as **shipped** after dogfooding it to draft `docs/spec/test-author.md`: its `spec-quality` self-check caught six findings in the first draft (a sequencing scenario, a vague Then, an unexercised input, a Goal/scenario redundancy, and two recommendation-less open questions) and it revised to a `ready` spec before returning. The dogfood doubles as the ready-made contract for building `test-author` next. Status updated in `ROADMAP.md` and `docs/CATALOG.md`. User sign-off recorded.
- [x] (2026-07-24) Closed `feat-0017`: authored the `spec-author` skill (ROADMAP Epic B #2), the first non-folded spine skill, by extracting the drafting discipline from the upstream RPCE Spec workflow into a portable `SKILL.md`. It drafts the seven-section scenario-based spec, composes `spec-quality` to self-check to `ready`, writes to `docs/spec/<slug>.md` as `status: draft`, stays read-only for implementation surfaces, and stops for human approval before `new-task` consumes it. Built against its own blessed contract (`docs/spec/spec-author.md`). Draft pending an in-kit dogfood (drafting the `test-author` spec) before blessing.
- [x] (2026-07-24) Blessed `spec-quality`, `spec-plan-readiness`, `test-quality`, and `spec-conformance` as **shipped** after dogfooding each on real in-kit work: `spec-quality` reviewed the new `docs/spec/spec-author.md` draft and caught five findings; `spec-plan-readiness` gated the `spec-author` build (spec plus a two-task decomposition) to `implementable` with a scenario-to-test map; `test-quality` produced a layer/oracle recommendation for the untested `scripts/validate-skills.py` and surfaced a testability issue; `spec-conformance` audited that script against `docs/spec/validate-skills.md` into a conformance matrix with one accepted divergence. This starts the kit dogfooding its own contract-driven spine, introducing a `docs/spec/` practice. Status updated in `ROADMAP.md` and `docs/CATALOG.md`. User sign-off recorded.
- [x] (2026-07-24) Closed `feat-0016`: folded `spec-conformance` into the kit (ported from `repoprompt-workflows`, Balarama Bosch, MIT; house-styled; states the boundary that it composes into `fix-batch` verification rather than duplicating it; cross-links the planned `verifier-agent`). Landed via a live `fix-batch` run (Sonnet worktree agent) and `reconcile-worktrees`. Draft pending an in-kit dogfood before blessing.
- [x] (2026-07-24) Closed `feat-0015`: folded `test-quality` into the kit (ported from `repoprompt-workflows`, Balarama Bosch, MIT; house-styled; cross-links the planned `test-author`). Landed via the same `fix-batch` run and `reconcile-worktrees`. Draft pending an in-kit dogfood before blessing.
- [x] (2026-07-24) Closed `feat-0014`: folded `spec-plan-readiness` into the kit (ported from `repoprompt-workflows`, Balarama Bosch, MIT; house-styled; retargeted from RPCE "Deep Plan" to the kit's ROADMAP-Feature-plus-`.tasks/` model; cross-links `spec-quality`). Landed via the same `fix-batch` run and `reconcile-worktrees`. Draft pending an in-kit dogfood before blessing.
- [x] (2026-07-24) Closed `feat-0013`: folded `spec-quality` into the kit (ported from `repoprompt-workflows`, Balarama Bosch, MIT; house-styled; cross-links the planned `spec-author`). First fold-in of the Epic B contract-driven spine, dispatched as a parallel `fix-batch` run of four Sonnet worktree agents and consolidated via `reconcile-worktrees`. Added the repo `NOTICE` recording upstream provenance. Draft pending an in-kit dogfood before blessing.
- [x] (2026-07-24) Blessed `doc-author` and `doc-revise` as **shipped** after authoring `docs/ARCHITECTURE.md` and using a consistency pass to align the README, catalog, roadmap, and status documentation. User sign-off recorded.
- [x] (2026-07-24) Blessed `agent-handoff` and `human-handoff` as **shipped** after the in-kit fold-in brief and partner-facing `docs/PROJECT-STATUS.md` dogfood use. User sign-off recorded.
- [x] (2026-07-24) Closed `feat-0012`: folded `doc-revise` into the kit (ported from `zen-solutions-studio`, house-style default pointed at `.agents/rules/house-style.md`, `doc-author` cross-link added). Pure prose, no portability gating. Draft pending an in-kit dogfood use before blessing.
- [x] (2026-07-24) Closed `feat-0011`: folded `doc-author` into the kit (ported from `~/.claude/skills`, house-style default pointed at `.agents/rules/house-style.md`, `doc-revise`/`init-worktracking`/`project-bootstrap` cross-links added). Pure prose, no portability gating. Draft pending an in-kit dogfood use before blessing.
- [x] (2026-07-24) Closed `feat-0010`: folded `human-handoff` into the kit (ported from `zen-solutions-studio`, house-style default pointed at `.agents/rules/house-style.md`, `agent-handoff` cross-link added). Pure prose, no portability gating. Draft pending an in-kit dogfood use before blessing.
- [x] (2026-07-24) Closed `feat-0009`: folded `agent-handoff` into the kit (ported from `~/.claude/skills`, house-style default pointed at `.agents/rules/house-style.md`, `human-handoff` cross-link added). Pure prose, no portability gating. Already dogfooded by producing this fold-in's handoff brief; draft pending user sign-off before blessing.
- [x] (2026-07-24) Closed `feat-0008`: iterated `code-review` from the scripts dogfood, making path-scoped whole-file review a first-class Step 1 mode alongside change review.
- [x] (2026-07-24) Closed `bug-0001`: fixed the `code-review` findings in the kit scripts. `install.py` copy-mode symlink ownership now uses exact path equality instead of a string-prefix test (a real clobber-safety bug), dropped dead `remaining` code, hardened `is_managed`, and documented the copy-mode manifest dependency; `build-adapters.py` now emits the skill description as a JSON-serialized (valid YAML) scalar, fixing malformed adapter frontmatter for descriptions containing colons or quotes. Re-review came back clean.
- [x] (2026-07-24) Blessed `code-review` as **shipped** after dogfooding it on this repo's own change; status updated in `ROADMAP.md` and `docs/CATALOG.md`.
- [x] (2026-07-24) Closed `feat-0007`: built the `code-review` skill and its swappable `review-quality` lens (8-category rubric; blocker/major/minor/nit; report-only; findings validated before reporting; reuses `pr-describe`'s range logic). Adopts moonray's composable quality-lens pattern. Dogfooded on this repo's own change (caught and fixed a house-style em-dash). Draft pending further field iteration.
- [x] (2026-07-24) Blessed `fix-batch` and `reconcile-worktrees` as **shipped** after a live in-kit run: dispatched two parallel worktree-isolated agents via `fix-batch`, verified each against real diffs, and reconciled both into main via `reconcile-worktrees`. Status updated in `ROADMAP.md` and `docs/CATALOG.md`.
- [x] (2026-07-24) Closed `chore-0002`: aligned `docs/CATALOG.md` Tier C Content OS names with `ROADMAP.md` Epic C (executed via the `fix-batch` live run).
- [x] (2026-07-24) Closed `chore-0001`: fixed the README workflow-spine diagram order, removed the stale installer note, and unlinked a broken placeholder link (executed via the `fix-batch` live run).
- [x] (2026-07-24) Closed `feat-0006`: folded `reconcile-worktrees` into the kit (ported from `~/.claude/skills`, house-styled, wired into the `.tasks/` spine). Draft pending a live in-kit run before blessing.
- [x] (2026-07-24) Closed `feat-0005`: folded `fix-batch` into the kit (ported from `~/.claude/skills`, house-styled, portability-gated with a labeled Claude Code section, wired into the `.tasks/` spine). Draft pending a live in-kit run before blessing.
- [x] (2026-07-24) Blessed `pr-describe` as **shipped** after a first field-iteration against this kit's own working tree; status updated in `ROADMAP.md` and `docs/CATALOG.md`.
- [x] (2026-07-24) Closed `feat-0004`: iterated `pr-describe` from dogfooding, added a working-tree fallback so it describes uncommitted work on the default branch instead of dead-ending when the branch is not ahead of base.
- [x] (2026-07-24) Closed `feat-0003`: drafted the first version of the `pr-describe` skill (PR body + changelog entry from a branch's diff; draft-only, never touches GitHub; changelog format by inspection; branch-vs-merge-base range). Lints clean via `scripts/validate-skills.py`. Marked draft in `ROADMAP.md`/`docs/CATALOG.md`, pending field iteration before it is blessed.
- [x] (2026-07-24) Blessed `project-bootstrap` as **shipped** after review; status updated in `ROADMAP.md` and `docs/CATALOG.md`.
- [x] (2026-07-24) Closed `feat-0002`: iterated `project-bootstrap` with a swappable house code-style layer under `templates/` (ruff, prettier, eslint, editorconfig + doc), folding in the four settled design decisions. Never-clobber preserved. Both validators green.
- [x] (2026-07-23) Closed `feat-0001`: drafted the first version of the `project-bootstrap` skill. Lints clean via `scripts/validate-skills.py`. Marked draft in `ROADMAP.md`, pending field iteration before it is blessed as shipped.

### Seeded provenance (predates this tracker)

The following shipped during the kit's initial build, before this tracking system existed. Recorded here for a complete ledger.

- (2026-07-23) Established the tracking system for this repo (`AGENTS.md`, `.tasks/`, `ROADMAP.md`, `CHANGELOG.md`), dogfooding `init-worktracking` at team tier.
- (2026-07-23) Shipped the installer and adapter tooling: `scripts/install.py`, `scripts/build-adapters.py`, `scripts/validate-skills.py`.
- (2026-07-23) Added the `new-task` authoring skill.
- (2026-07-23) Hardened `init-worktracking` (seven improvements) and stood up the kit repository.

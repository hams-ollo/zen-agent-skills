"""Local reporting over the session corpus this repository's own agent runs produce.

Maintainer tooling. Nothing here is placed into an adopter's tree by `install.py`, which is
the scope the "Out of scope by design" rule in `ROADMAP.md` was narrowed to on 2026-08-28
(`chore-0076`). The standard-library-only rule in `AGENTS.md` still governs this package.

Contract: `docs/spec/agent-observatory.md`.
"""

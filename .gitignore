# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Installer artifacts (per-machine, never committed)
scripts/.install-manifest.json

# Throwaway install home used by the documented preview commands and by CI
.tmp/

# Node (only if adapter tooling ever needs it)
node_modules/

repoprompt-workflows-main
# Agent worktrees created by fix-batch (per-run scratch, never committed)
.claude/worktrees/

# Observatory store: derived data, rebuildable from the corpus (feat-0053)
.observatory/

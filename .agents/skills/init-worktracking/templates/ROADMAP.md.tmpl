# Roadmap

**Status:** living document | **Last updated:** {{DATE}}

The strategic view: what's built, what's in progress, and what's planned. For the atomic, agent-assignable work items, see [`.tasks/`](.tasks/); for completed work, see [`CHANGELOG.md`](CHANGELOG.md). For the full altitude model (Epic -> Feature -> Task), see [`AGENTS.md`](AGENTS.md) section 1.

---

## Current state

{{CURRENT_STATE}}

<!--
Describe what exists today, organized by theme, phase, or subsystem, whatever
fits this project. This is the 30,000 ft Epic layer: durable areas of the work,
not individual tasks.
-->

---

## Forward plan

Ordered roughly by effort-to-value. Each scoped item is the 10,000 ft Feature layer; when a Feature is ready to build, decompose it into one or more task files in [`.tasks/`](.tasks/) and link them here. Strike a Feature through when all its child tasks reach `.tasks/done/`.

1. **Feature name.** Why it matters and when. Decomposed into `.tasks/feat-000N-...`.
2. ~~Done feature.~~ **Done (YYYY-MM-DD).** One line on what shipped.

---

## Out of scope by design

- Things deliberately not being built, so nobody re-proposes them as gaps.

# Changelog

Completed work for {{PROJECT_NAME}}, newest first. This is the append-only ledger for the spec-driven task workflow: when a task in [`.tasks/`](.tasks/) is finished, its file moves to `.tasks/done/` and one dated line is added here referencing the task id.

---

## Task log

<!-- Newest entries at the top. One line per completed task, e.g.:
- [x] (YYYY-MM-DD) Closed `feat-0001`: one-line description of what shipped and where.
-->
